<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Allah99Name" parent="Theme.MaterialComponents.Light.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorOnPrimary">@color/greenSimple</item>
        <item name="colorPrimary">@color/white</item>

        <!-- Secondary brand color. -->
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" >#347C37</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="ThemeOverlay.PopupTheme." parent="Theme.AppCompat.Light">
        <!-- Primary brand color. -->
        <item name="android:colorBackground">@color/greenSimple</item>
        <item name="android:textColor">@color/white</item>
    </style>


</resources>